/**
 * Choosen by fair call to https://www.random.org/passwords/?num=1&len=12&format=plain&rnd=new
 * Guaranteed to be random
 * @see https://xkcd.com/221/
 * Also, React doesn't support keying context with Symbol so we use a random prefixed string instead.
 * Google says it's unused so it shouldn't clash with anything.
 */
export default '$nexusq76R2eZzzuFq';
